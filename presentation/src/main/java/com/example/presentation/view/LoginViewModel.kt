package com.example.presentation.view

import es.example.presentation.BaseViewModel

class LoginViewModel: BaseViewModel<LoginState, LoginTransition>() {
    //private val repository = Repository()
    //private val doLogin = DoLogin(repository)

    fun doLogin() {
        //UC-Login
    }
}